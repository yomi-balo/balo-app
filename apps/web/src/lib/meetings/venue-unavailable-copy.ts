/**
 * The ONE wording for a meeting that ended `venue_unavailable`: its call room was never ready, so
 * nobody could join. Balo's failure — both lenses read the same words; no party is named, never
 * the reader, never second-person blame. Shared by the case row and the recap's not-held panel so
 * they cannot drift. ⚠ MJ COPY CHECKPOINT — pending MJ sign-off (flag in the PR body).
 */
export const VENUE_UNAVAILABLE_LABEL = 'Call room unavailable';
export const VENUE_UNAVAILABLE_NOTE = "Our call room wasn't ready in time — this one's on us";
export const VENUE_UNAVAILABLE_RECAP_BODY =
  "Our call room wasn't ready in time, so this call couldn't go ahead. That's on us, not on anyone who was booked — sorry.";
