/**
 * BAL-396 §8.5 — the ONE calendar help link, shared by every "learn more" surface in the
 * calendar tab. Replaces three dead `docs.cronofy.com` hrefs (Cronofy's own docs, never
 * Balo's) with a single Balo-owned page so the URL is a one-line change, not three.
 *
 * ⚠ NOT an unverified `docs.apiroc.com` substitute — §18 open question 1 of the BAL-396
 * technical plan. This value is a placeholder pending an ops task to publish the page; update
 * it here, once, when that page exists.
 */
export const CALENDAR_HELP_URL = 'https://balo.expert/help/calendar-connection';
